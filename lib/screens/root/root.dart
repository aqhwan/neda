import 'package:flutter/material.dart';

class NedaRoot extends StatelessWidget {
  const NedaRoot({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Neda')));
  }
}
